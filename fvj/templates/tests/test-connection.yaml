apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fvj.fullname" . }}-test-connection"
  labels:
    {{- include "fvj.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fvj.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
