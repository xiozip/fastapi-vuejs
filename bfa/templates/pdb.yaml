apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "bfa.fullname" . }}
  labels:
    {{- include "bfa.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "bfa.selectorLabels" . | nindent 6 }}
