apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bfa.fullname" . }}-test-connection"
  labels:
    {{- include "bfa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bfa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
